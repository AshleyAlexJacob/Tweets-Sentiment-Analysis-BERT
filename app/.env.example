# Leave empty during local dev to use Vite proxy (see vite.config.js → /v1 → FastAPI).
# For production build against a remote API, set the full origin, e.g.:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
